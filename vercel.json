{
    "buildCommand": "cd numerix && npm install && npm run build",
    "outputDirectory": "numerix/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}